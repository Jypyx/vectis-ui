import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as VBreadcrumbStories from './VBreadcrumb.stories'

<Meta of={VBreadcrumbStories} />

# Breadcrumb

Fil d'Ariane data-driven : un `<nav>` + liste ordonnée entièrement piloté par la prop
`items: BreadcrumbItem[]` (`{ label, href, iconStart? }`, type exporté) — pure dérivation
de données, aucune API navigateur, SSR-safe. Chaque item est un lien ; l'item **actif** est
dérivé de `currentPath` : celui dont le `href` correspond (slash final normalisé) reste un
lien cliquable et reçoit `aria-current="page"` — pattern ARIA APG.

Les séparateurs sont des `<VIcon>` décoratifs (`aria-hidden`) placés en tête de chaque item,
le premier étant masqué en CSS (`:first-child`).

<Canvas of={VBreadcrumbStories.Default} />

<Controls of={VBreadcrumbStories.Default} />

## Séparateur

`separator` (défaut `chevron_right`) accepte un nom d'icône **ou** un rendu explicite
(`{ src: '/chevron.svg' }`) : une chaîne est toujours un nom, jamais une URL devinée. Note
RTL : une icône directionnelle ne se retourne pas
automatiquement.

<Canvas of={VBreadcrumbStories.SeparateurIcone} />

<Canvas of={VBreadcrumbStories.SeparateurImage} />

## Icônes d'items

`iconStart` sur un item place une icône avant son libellé — même détection nom/URL que le
séparateur. Les icônes suivent la taille et la couleur du texte via le contexte
`--vectis-icon-size`.

<Canvas of={VBreadcrumbStories.AvecIcones} />

## Troncature

Au-delà de `maxItems` (minimum effectif : 3), la liste affiche le 1er item + un bouton
« … » + l'avant-dernier + le dernier. Le bouton ouvre un VMenu listant uniquement
les items masqués, rendus en vrais liens (`<a role="menuitem">`). Clavier : flèches pour
naviguer, Échap referme et rend le focus au déclencheur.

<Canvas of={VBreadcrumbStories.Tronque} />

## Cas limites

Les libellés longs sont tronqués par ellipsis, la liste passe à la ligne si nécessaire :

<Canvas of={VBreadcrumbStories.LibellesLongs} />

## Accessibilité

- `<nav aria-label="Fil d'Ariane">` (personnalisable via `label`) + liste ordonnée : les
  lecteurs d'écran annoncent la navigation et la position de chaque segment.
- L'item actif reste un lien avec `aria-current="page"` — il demeure atteignable et
  identifiable (pattern APG).
- Les séparateurs sont purement décoratifs (`aria-hidden`), jamais annoncés.
- Le bouton d'ellipsis a un libellé explicite (`ellipsisLabel`, « Afficher les pages
  intermédiaires » par défaut) et son menu suit le pattern ARIA menu (roving focus).
- Le séparateur par défaut est un SVG embarqué ; les noms d'icônes que VOUS fournissez
  viennent de votre police — prévoir un repli acceptable (l'icône est décorative, le
  libellé porte le sens).

Les libellés accessibles de ce composant viennent du dictionnaire du design system : ils se traduisent globalement (voir **Fondations › Internationalisation**), et les props texte restent prioritaires pour un ajustement au cas par cas.
