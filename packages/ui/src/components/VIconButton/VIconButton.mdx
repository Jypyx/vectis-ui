import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as VIconButtonStories from './VIconButton.stories'

<Meta of={VIconButtonStories} />

# IconButton

Bouton à icône seule : un wrapper de `VButton` rendu **carré** (largeur = hauteur, padding
supprimé), avec la même API visuelle. La prop `label` est **obligatoire** — elle pose
l'`aria-label` du bouton, car une icône seule ne fournit aucun nom accessible aux lecteurs
d'écran.

L'icône peut être fournie de deux façons : par la prop `icon` (nom d'icône ou rendu explicite, sans
passer par le slot — le remplissage se pilote alors via `icon-filled`), ou dans le slot par
défaut pour du contenu custom (le slot ne s'affiche qu'en l'absence de `icon`). Via le
composant `VIcon`, la taille et l'axe `opsz` suivent automatiquement la taille du bouton
(xs → 16px, sm/md → 20px, lg/xl → 24px).

<Canvas of={VIconButtonStories.Default} />

<Controls of={VIconButtonStories.Default} />

## Variants et tones

Mêmes cinq variants (`solid`, `outline`, `ghost`, `elevated`, `tonal`) et cinq tones
(`accent`, `neutral`, `danger`, `success`, `warning`) que `VButton` — voir sa documentation
pour le détail de chaque rendu. Défauts adaptés à l'usage icône : `variant="ghost"` et
`tone="neutral"` (le VIconButton est typiquement une action discrète de barre d'outils).

<Canvas of={VIconButtonStories.Variants} />

## Tailles

Cinq tailles carrées (`xs` 24px, `sm` 32px, `md` 40px, `lg` 48px, `xl` 56px) ; `compact`
réduit le côté de 4px (20/28/36/44/52px) sans changer la taille de l'icône. La largeur suit
automatiquement la hauteur du `VButton` sous-jacent, compact inclus.

<Canvas of={VIconButtonStories.Sizes} />

## Icône par prop

La prop `icon` prend un nom d'icône (ou un rendu explicite) et rend l'`VIcon` en interne, sans
passer par le slot ; `icon-filled` demande la variante pleine (axe `FILL`).

<Canvas of={VIconButtonStories.IconProp} />

## Sources d'icône

Le slot (fallback en l'absence de `icon`) accepte les trois sources du composant `VIcon` :
SVG embarqué du DS, ligature de la police du consommateur (voir le README), SVG inline, ou
image.

<Canvas of={VIconButtonStories.IconsTypes} />

## États

`loading` désactive le bouton, affiche le spinner et pose `aria-busy` :

<Canvas of={VIconButtonStories.Loading} />

`disabled` bascule tous les variants sur des nuances de gris, comme `VButton` :

<Canvas of={VIconButtonStories.Disabled} />

## Accessibilité

- `label` obligatoire → `aria-label` : le bouton a toujours un nom accessible ; l'icône,
  elle, reste décorative (`aria-hidden`).
- Pour un bouton avec libellé visible **et** icône, utiliser `VButton` avec
  `icon-start`/`icon-end` plutôt qu'un VIconButton.
- Focus visible par anneau (`--vectis-focus-ring-*`), transitions soumises à
  `prefers-reduced-motion` — hérités de `VButton`.
