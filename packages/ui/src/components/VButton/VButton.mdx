import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks'
import * as VButtonStories from './VButton.stories'

<Meta of={VButtonStories} />

# Button

Bouton d'action construit sur le `<button>` natif (focus, clavier et désactivation gratuits) —
ou rendu en lien `<a>` via la prop `href`. Aucun JavaScript de comportement, à une exception
près : le pont « lien inerte » (voir [Lien](#lien)).

<Canvas of={VButtonStories.Default} />

<Controls of={VButtonStories.Default} />

## Variants et tones

Cinq variants (`solid`, `outline`, `ghost`, `elevated`, `tonal`) croisés avec cinq tones
(`accent`, `neutral`, `danger`, `success`, `warning`). Les tones ne définissent que des
variables CSS locales (`--tone-bg-solid`, `--tone-text-tinted`…) que les variants consomment : toute
combinaison est valide.

- **solid** : fond plein, action principale.
- **outline** / **ghost** : bordure teintée / texte seul, actions secondaires.
- **elevated** : surface surélevée avec ombre (style Material 3), texte teinté ; l'ombre
  monte au survol et retombe au press.
- **tonal** : fond teinté doux + texte teinté, entre solid et ghost.

<Canvas of={VButtonStories.Variants} />

## Tailles

Cinq tailles (`xs` 24px, `sm` 32px, `md` 40px, `lg` 48px, `xl` 56px, via les tokens
`--vectis-control-height-*`). La prop `compact` réduit la hauteur de 4px (20/28/36/44/52px)
**sans toucher** au padding, à la typographie ni à la taille des icônes. Le mapping
taille → hauteur/typo/icône/padding est partagé par tous les contrôles du design system
(classe `v-control`, voir `styles/control-size.css`).

<Canvas of={VButtonStories.Sizes} />

## Icônes

Les props `icon-start` / `icon-end` prennent un nom
[Material Symbols Rounded](https://fonts.google.com/icons) et rendent le composant `VIcon` en
interne. La taille et l'axe `opsz` de l'icône suivent automatiquement la taille du bouton
(xs → 16px, sm/md → 20px, lg/xl → 24px). Les icônes sont décoratives (`aria-hidden`) : le nom
accessible du bouton reste son libellé.

> Les icônes rendues par le DS lui-même sont des SVG embarqués : aucune police n'est requise.
> Vos propres noms d'icônes, eux, viennent de la police ou de la bibliothèque que vous
> branchez (voir le README).

<Canvas of={VButtonStories.Icons} />

La prop `icon-filled` remplit les icônes `icon-start` / `icon-end` (axe `FILL` de la police),
sans avoir à passer par les slots :

<Canvas of={VButtonStories.IconsFilled} />

Les slots `#start` / `#end` restent disponibles pour du contenu custom (SVG inline, image) et
**priment** sur les props (leur remplissage se pilote alors directement sur le `VIcon` via sa
prop `filled`) :

<Canvas of={VButtonStories.IconsTypes} />

## Lien

Avec `href`, le composant rend un `<a>` : `type` et `disabled` natifs ne sont pas posés, les
attributs de lien (`target`, `rel`…) passent en fallthrough.

<Canvas of={VButtonStories.Link} />

Un `<a>` n'ayant pas de `disabled` natif, un lien `disabled` (ou `loading`) devient un
**lien inerte** : `href` retiré (non focusable, aucune navigation), `aria-disabled="true"`
pour les technologies d'assistance, handlers `click` filtrés. C'est le seul JavaScript de
comportement du composant.

<Canvas of={VButtonStories.DisabledLink} />

## États

`loading` désactive le bouton, affiche un spinner, pose `aria-busy` et atténue le bouton en
opacité (il garde ses couleurs). Le spinner **remplace** l'emplacement de début
(`icon-start` / slot `#start`) — jamais de spinner et d'icône côte à côte ; `icon-end` reste
affiché :

<Canvas of={VButtonStories.Loading} />

`disabled` bascule tous les variants sur des nuances de gris (`--vectis-color-surface-muted`,
`--vectis-color-text-subtle`) — pas de jeu d'opacité, et le rendu suit automatiquement le thème
dark :

<Canvas of={VButtonStories.Disabled} />

## Accessibilité

- Focus visible par anneau (`--vectis-focus-ring-*`), jamais supprimé.
- Icônes décoratives : le nom accessible est toujours le libellé du slot par défaut. Pour un
  bouton à icône seule, utiliser `VIconButton` (libellé `aria-label` obligatoire).
- `loading` → `aria-busy="true"` ; lien inerte → `aria-disabled="true"` sans `href`.
- Les transitions respectent `prefers-reduced-motion`.
